rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for live scorecards, leaderboards, and user profiles
    // Write access for any authenticated Google user
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}